<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title -E_Kmao is asp.net Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>

    <div class="navbar navbar-inverse " style="background-color: #FF8C00">
        <div class="container">
            <div class="navbar-header">



            </div>
            <div class="navbar-static-top">
                <ul class="nav navbar-nav">

                    <li>@Html.ActionLink("Home", "Index", "Home")</li>

                    <li>@Html.ActionLink("Rules", "Rnavbar-fixed-topules", "Home")</li>
                    <li>@Html.ActionLink("News Feed", "Contact", "Home")</li>
                    <li>@Html.ActionLink("Register Now", "CustomerRegistration", "Customer")</li>

                    <li>@Html.ActionLink("Make Complain", "Contact", "Home")</li>
                    <li>@Html.ActionLink("About", "About", "Home")</li>
                    <li>@Html.ActionLink("Contact", "Contact", "Home")</li>


                </ul>
            </div>

            <div class="navbar-right">
                <ul class="nav navbar-nav">

                    <li>
                        @if (User.Identity.IsAuthenticated)
                        {
                            if (Session["FirstName"] != null)
                            {
                                <h4>
                                    @Session["FirstName"].ToString() @Session["LastName"].ToString()
                                </h4>

                            <li>
                                @using (@Html.BeginForm("Logout", "Customer", FormMethod.Post, new { id = "logoutForm" }))
                                {
                                    <a href="javascript:document.getElementById('logoutForm').submit()">Logout</a>



                                }




                            </li>

                        }

                    }

                    else

                    {
                        <h4>
                            <a href="~/Controllers/CustomerController.cs"> Login </a>
                        </h4>
                    }
                        </li>

                        <li>

                          

                        </li>


                    </ul>
                </div>

            </div>
        </div>
        <div class="container body-content">
            @RenderBody()
            <hr />
            <footer>
                <p>&copy; @DateTime.Now.Year - All right reserved to the Software Studio </p>
            </footer>
        </div>

        @Scripts.Render("~/bundles/jquery")
        @Scripts.Render("~/bundles/bootstrap")
        @RenderSection("scripts", required: false)

    </body>
</html>
@section scripts{
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.0/jquery.min.js"></script>

    <script src="assets/plugins/bootbox/bootbox.min.js" type="text/javascript"></script>
    <script type="text/javascript">




        $(document).ready(function () {
            $("#submit").click(function (e) {
                $.ajax({
                    type: "POST",
                    url: "/Customer/Logout",
                    dataType: "html",
                    success: function (result, status, xhr) {


                        alert(result);
                    },
                    error: function (xhr, status, error) {
                        $("#dataDiv").html("Result: " + status + " " + error + " " + xhr.status + " " + xhr.statusText)
                    }
                });

                return false;
            });
        });




        function drawStuff(menu) {
            alert(menu.value);

            setTimeout('', 1000);
            window.location.href = 'http://localhost:52430/Professional/Pro_Index/' + menu.value;

        }




                        /*
                        bootbox.prompt("What is your name?", function (result) {

                        })



                        bootbox.dialog({
                        message: '<div class="text-center"><i class="fa fa-spin fa-spinner"></i> Loading...</div>',
                        closeButton: false
                        })*/

    </script>
}